#pragma once

#include <Arduino.h>

namespace RobotUtils {
    void shutdown() {
        // TODO: This function should safely stop all functions on the robot (turn off all pins and motors).
    }
}